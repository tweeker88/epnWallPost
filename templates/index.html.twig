{% extends 'base.html.twig' %}

{% block body %}
    {% for item in items %}
        {{ dump(items) }}
        <ul>
            <li><a href="{{ item.url }}" target="_blank">{{ item.name }}</a></li>
        </ul>
    {% endfor %}
{% endblock %}